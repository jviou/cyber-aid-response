services:
  app:
    build: .
    container_name: crisis-app
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - CRISIS_STATE_DIR=/app/data
      - CRISIS_STATE_FILE=session-v2.json
