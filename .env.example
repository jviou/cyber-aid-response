# Port d'écoute de l'application (Défaut: 8080)
PORT=8080

# Dossier de stockage des données persistantes (Défaut: /app/data dans le conteneur)
# Ne changez ceci que si vous savez ce que vous faites avec les volumes Docker
CRISIS_STATE_DIR=/app/data

# Nom du fichier de session (Défaut: session-v2.json)
CRISIS_STATE_FILE=session-v2.json

# Environnement (production/development)
NODE_ENV=production
